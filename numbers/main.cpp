#include <iostream>

#include "rational.h"
#include "real.h"

int main()
{
    /*
    Rational
            q1(-1, 2),
            q2(-3, 4);
    std::cout << q1 + q2 << "\n" <<
                 q1 - q2 << "\n" <<
                 q1 * q2 << "\n" <<
                 q1 / q2 << "\n";

    Real
            r1(Rational(141, 100), Rational(142, 100)),
            r2(Rational(1415, 1000), Rational(1425, 1000));

    std::cout << r1 + r2 << "\n" <<
                 r2 - r1 << "\n" <<
                 r1 * r2 << "\n" <<
                 r1 / r2 << "\n" <<
                 (r1 == r2) << "\n" <<
                 (r1 < r2) << "\n" <<
                 (r1 > r2) << std::endl;

    Real r;
    r.byCenterAndRadius({2, 5}, {3, 100});
    */

    return 0;
}
